---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: busybox

inputs:
  - name: concourse-packer-ci
  - name: concourse-release

outputs:
  - name: write-file

run:
  path: concourse-packer-ci/tasks/write-file.sh

params:
  CONTENT:
  FILE_NAME:
